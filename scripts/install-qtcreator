# install-qtcreator
# IDE - Install qtCreator IDE
# Install IDE for qt programs
echo -e "Running: IDE - Install qtCreator IDE - ./scripts/install-qtcreator"

sudo apt-get install -y qtcreator

if [ $? -eq 0 ]; then
   echo -e "\\nOK\\n"
else
   echo -e "\\nFAIL\\n"
fi
